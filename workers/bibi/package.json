{
  "name": "biblion-bibi-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev",
    "test": "node --test test/*.test.mjs",
    "secret:nvidia": "wrangler secret put NVIDIA_API_KEY",
    "secret:openai-compatible": "wrangler secret put OPENAI_COMPATIBLE_API_KEY",
    "secret:firebase-web-api-key": "wrangler secret put FIREBASE_WEB_API_KEY",
    "eval:models": "node evals/model_eval.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
